%p#info
  %h1 KI Wunderland
  Klick auf die Kamera um ein Foto aufzunehmen.

%form#form{action: '.', method: 'POST', enctype: 'multipart/form-data'}
  .custom-file
    #cam
    %input#file{type: 'file', name: 'upload', accept: 'image/*', capture: true}

#progress
  .pie-wrapper
    %span.label
      %span#progress-number> 0
      %span.smaller %
    #pie.pie
      #left-side.half-circle
      #right-side.half-circle
    .pie-bg

%p#message
